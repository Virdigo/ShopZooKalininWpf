<Page x:Class="ShopZooKalininWpf.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShopZooKalininWpf.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login" Background="White">

    <Grid>
        <StackPanel>
            <Label HorizontalAlignment="Center" FontSize="35"> Авторизация </Label>
            <Label FontSize="18">Введите логин</Label>
            <TextBox x:Name="UsernameTextBox"  Margin="10"/>
            <Label FontSize="18"> Введите пароль</Label>
            <TextBox x:Name="PasswordBox"  Margin="10"/>

            <Button Content="Войти" Click="LoginButton_Click" Margin="10" FontSize="18" Background="Wheat"/>
            <Button Content="Посмотреть без авторизации" Click="VhodClick" Margin="10" FontSize="18" Background="White"/>
        </StackPanel>
    </Grid>
</Page>
